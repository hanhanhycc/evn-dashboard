# Example env file — copy to `.env` and adjust if needed.
# The app does not require any secrets; users log in with their own EVN account
# through the web UI. Only PORT is read at startup.
PORT=3000
